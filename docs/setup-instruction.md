GitHub Repository:
https://github.com/Prajwalbadiger76/RevConnect-CAPSTONE/tree/main

--------------------------------
1. SYSTEM REQUIREMENTS
--------------------------------
• Java JDK 17 or above  
• Maven 3.8+  
• Oracle XE Database  
• Spring Tool Suite / Eclipse / IntelliJ  
• Git  

--------------------------------
2. CLONE THE PROJECT
--------------------------------

git clone https://github.com/Prajwalbadiger76/RevConnect-CAPSTONE.git


--------------------------------
3. IMPORT PROJECT IN IDE
--------------------------------

For Eclipse / STS:

1. Open Eclipse / Spring Tool Suite
2. File → Import
3. Existing Maven Project
4. Select the project folder
5. Click Finish

--------------------------------
4. PROJECT STRUCTURE
--------------------------------

RevConnect-CAPSTONE

src/main/java
→ Controllers
→ Services
→ Repositories
→ Entities
→ Config

src/main/resources
→ application.properties

pom.xml
README.md
SETUP-INSTRUCTIONS.md

--------------------------------
5. DATABASE SETUP
--------------------------------

Login to Oracle:

sqlplus system/<password>@localhost:1521/XEPDB1

--------------------------------
Create User
--------------------------------

CREATE USER revconnect IDENTIFIED BY rev123;

GRANT CONNECT, RESOURCE TO revconnect;

ALTER USER revconnect QUOTA UNLIMITED ON USERS;

--------------------------------
Create Tables
--------------------------------

USERS
-----

CREATE TABLE users (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    username VARCHAR2(100),
    email VARCHAR2(100) UNIQUE,
    password VARCHAR2(255),
    bio VARCHAR2(300),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

POSTS
-----

CREATE TABLE posts (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id NUMBER,
    content VARCHAR2(1000),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

COMMENTS
--------

CREATE TABLE comments (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    post_id NUMBER,
    user_id NUMBER,
    comment_text VARCHAR2(500),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

LIKES
-----

CREATE TABLE likes (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    post_id NUMBER,
    user_id NUMBER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

FOLLOWS
-------

CREATE TABLE follows (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    follower_id NUMBER,
    following_id NUMBER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

--------------------------------
6. APPLICATION CONFIGURATION
--------------------------------

Open the file:

src/main/resources/application.properties

Update database configuration:

spring.datasource.url=jdbc:oracle:thin:@//localhost:1521/XEPDB1
spring.datasource.username=revconnect
spring.datasource.password=rev123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

--------------------------------
7. INSTALL DEPENDENCIES
--------------------------------

Run Maven:

mvn clean install

--------------------------------
8. RUN APPLICATION
--------------------------------

Method 1 – Using Maven

mvn spring-boot:run

Method 2 – Using IDE

1. Open RevHireApplication.java
2. Right Click
3. Run As → Spring Boot App

--------------------------------
9. VERIFY APPLICATION
--------------------------------

Open browser:

http://localhost:5050

If the server starts successfully, setup is complete.

--------------------------------
10. GIT WORKFLOW
--------------------------------

Switch to develop branch:

git checkout develop
git pull origin develop

Create feature branch:

git checkout -b feature/your-feature-name

Push branch:

git push -u origin feature/your-feature-name

--------------------------------
11. COMMIT AND PUSH
--------------------------------

git add .
git commit -m "Implemented feature"
git push

--------------------------------
12. CREATE PULL REQUEST
--------------------------------

Go to GitHub → Pull Requests

Base Branch:
develop

Compare Branch:
feature/your-feature-name

Click:
Create Pull Request → Merge Pull Request

--------------------------------
13. IMPORTANT RULES
--------------------------------

Do NOT push directly to main  
 Do NOT push directly to develop  

 Always create feature branches  
 Use clear commit messages  
 Resolve conflicts before merging  

--------------------------------
14. WORKFLOW SUMMARY
--------------------------------

develop → feature/xyz → develop → main

Only the Team Lead merges develop into main.

--------------------------------
END OF SETUP
--------------------------------